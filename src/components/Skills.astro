---
import { type Locale, t } from '../i18n/index';
import { skillCategories } from '../data/skills';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const i = t(locale);

const categoryLabels: Record<string, string> = {
  frontend: i.skills.frontend,
  backend: i.skills.backend,
  cloud: i.skills.cloud,
  database: i.skills.database,
  architecture: i.skills.architecture,
  fundamentals: i.skills.fundamentals,
  methodologies: i.skills.methodologies,
};
---

<section id="skills" class="section" aria-labelledby="skills-heading">
  <div class="container">
    <h2 id="skills-heading" class="text-[length:var(--font-size-headline-lg)] font-bold text-[color:var(--color-on-surface)] dark:text-[color:var(--color-dark-on-surface)]">
      {i.skills.heading}
    </h2>
    <div class="mt-[var(--spacing-2xl)] grid gap-[var(--spacing-lg)] sm:grid-cols-2 lg:grid-cols-3">
      {skillCategories.map((category) => (
        <div class="card sm:last:col-span-2 sm:last:justify-self-center sm:last:max-w-[calc(50%-var(--spacing-lg)/2)] lg:last:col-span-1 lg:last:col-start-2 lg:last:max-w-none">
          <h3 class="text-[length:var(--font-size-title-md)] font-semibold text-[color:var(--color-primary)] dark:text-[color:var(--color-dark-primary)]">
            {categoryLabels[category.key]}
          </h3>
          <ul class="mt-[var(--spacing-md)] flex flex-wrap gap-[var(--spacing-sm)]" role="list">
            {category.items.map((skill) => (
              <li>
                <span class="chip">{skill}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
